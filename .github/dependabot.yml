version: 2
updates:
  # Keep the GitHub Actions used by CI up to date (security + version bumps).
  - package-ecosystem: github-actions
    directory: "/"
    schedule:
      interval: weekly
    commit-message:
      prefix: ci
